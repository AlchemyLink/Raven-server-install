---
# Copy to secrets.yml and encrypt with ansible-vault:
#   ansible-vault encrypt roles/relay/defaults/secrets.yml

relay_upstream_host: "1.2.3.4"   # EU server IP address
relay_certbot_email: "admin@admin.com"
