---
- name: Bootstrap base system (packages, BBR, sysctl)
  hosts: "{{ target | default('all') }}"
  become: true
  become_method: sudo
  gather_facts: true

  roles:
    - role: srv_prepare
      tags:
        - srv_prepare
